{
  "name": "cuaderno-campo",
  "version": "0.1.0",
  "private": true,
  "description": "Field notebook and forcing-chamber reader for perennial fruit-tree collections: offline PWA, Excel in and out, no server.",
  "license": "MIT",
  "author": "Daniel González Palazón",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "reproduce": "tsx scripts/reproduce.ts"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
